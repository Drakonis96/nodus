# Research Assistant: composer and thinking effort

Reviewed against official provider documentation on 2026-09-12.

The composer uses the active vault accent, with a thinking slider and send/stop. Context and source filters remain available in the header. The slider supports keyboard navigation and exposes only useful choices for the selected model. New chats and model changes start at **Standard**. The composer has no context-plus or microphone control.

`ResearchChatRequest.thinkingEffort` is independent of `chatReasoning`, `ModelRef.reasoningEffort`, and Nodi. Both normal and streaming Research Assistant requests carry the same options, including citation retries. Standard explicitly disables thinking where supported; otherwise it selects the lowest legal level. Unsupported or unknown controls are not invented. Subscription catalogues and LM Studio's native catalogue are authoritative; missing catalogue capabilities cannot produce a graded slider.

| Provider | Contract used by Research Assistant |
| --- | --- |
| OpenAI | `reasoning_effort`. GPT-5 starts at `minimal`; o-series and GPT-6 at `low`; GPT-5.1–5.6 can use `none`. GPT-5.2–5.5 include `xhigh`; GPT-5.6 and GPT-6 include `max`. Pro/Codex families have their own floors. Non-reasoning models omit the parameter. [GPT-5.4](https://developers.openai.com/api/docs/models/gpt-5.4), [GPT-5.5](https://developers.openai.com/api/docs/models/gpt-5.5), [GPT-5.6](https://developers.openai.com/api/docs/models/gpt-5.6-sol), [GPT-6](https://developers.openai.com/api/docs/models/gpt-6-astra). |
| ChatGPT / Codex | Uses the App Server's exact `supportedReasoningEfforts`, including `ultra` when advertised. Standard chooses the lowest advertised value, overriding model-wide defaults for this surface only. |
| GitHub Copilot | Uses `supportedReasoningEfforts` from the installed official SDK catalogue and sends `reasoningEffort` on session creation. No setting when the catalogue advertises none. |
| Anthropic | Older thinking models use `thinking.type=enabled` and token budgets; 4.6+ use adaptive thinking with `output_config.effort`. Standard disables thinking where legal. Mythos/Fable require adaptive thinking and start at low. `xhigh` is omitted on 4.6 and Mythos Preview. [Thinking](https://platform.claude.com/docs/en/build-with-claude/thinking), [effort](https://platform.claude.com/docs/en/build-with-claude/effort). |
| Gemini | Uses the OpenAI-compatible `extra_body.google.thinking_config`. 2.5 Flash can use budget 0; 2.5 Pro receives a positive minimum. 3 Pro uses low/high; 3.1 Pro has medium; Flash families use minimal when supported, while 3.7/3.8 start at low. [Thinking](https://ai.google.dev/gemini-api/docs/generate-content/thinking), [compatibility](https://ai.google.dev/gemini-api/docs/openai). |
| DeepSeek | Explicit `thinking.type` toggle; V4 native effort choices low/high/max. Standard disables thinking. [Thinking mode](https://api-docs.deepseek.com/guides/thinking_mode/). |
| Xiaomi MiMo | Explicit `thinking.type` disabled/enabled; no invented graded levels. [Thinking control](https://platform.xiaomimimo.com/docs/en-US/usage-guide/passing-back-reasoning_content). |
| Groq / Cerebras | GPT-OSS uses low/medium/high and cannot disable thinking. Supported Qwen families use their own none/default or none/low/medium/high contract. Cerebras Kimi exposes no effective effort control. [Groq](https://console.groq.com/docs/reasoning), [Cerebras](https://inference-docs.cerebras.ai/capabilities/reasoning). |
| OpenRouter | Unified `reasoning` object, with known upstream floors retained. Unclassified models require catalogue reasoning support. The gateway maps requested effort to upstream values. Mandatory GLM 5.3 Flash starts at low. [Reasoning](https://openrouter.ai/docs/guides/best-practices/reasoning-tokens). |
| OpenCode Go | Model-specific controls are retained on each of its Chat Completions, Messages and Responses routes. GPT and DeepSeek have levels; Qwen/GLM have toggles; MiniMax/Kimi have no verified graded control. [Go endpoints](https://dev.opencode.ai/docs/go/). |
| Ollama | Native `think`: boolean for known hybrid families, low/medium/high for GPT-OSS. [Thinking](https://docs.ollama.com/capabilities/thinking). |
| LM Studio | Native `reasoning`, selected from the model's `capabilities.reasoning.allowed_options`; supports switches, graded levels, and mandatory-on models. [Catalogue](https://lmstudio.ai/docs/developer/rest/list), [chat](https://lmstudio.ai/docs/developer/rest/chat). |
| Custom / Nodus local | Custom endpoints use controls only for recognized OpenAI model IDs. Unknown endpoints/models and Nodus local retain their runtime behavior and expose no unverified slider levels. |

Thinking allowances leave room for visible output without increasing a local model's fitted context window. Sampling parameters are omitted where reasoning makes them invalid. Compatibility retries retain the thinking control; a provider rejection is surfaced rather than silently switching effort. Local runtimes must offer their native API to honor advertised controls.

Validation: `scripts/test-research-reasoning.mjs`, `scripts/test-research-reasoning-transport.mjs`, and `scripts/verify-research-assistant.mjs` (renderer harness on port 5198). Provider requests are verified with local test endpoints and the actual SDK; no live paid inference is required. Browser screenshots are written to `artifacts/research-assistant/`.

The offline compatibility audit covers **23 OpenAI model IDs at every selectable effort**, from GPT-4o/4.1 through o1/o3/o4, GPT-5 (including mini, nano and a dated snapshot), GPT-5.1–5.5, GPT-5.6 Sol/Terra/Luna and GPT-6 Astra. Expected contracts live in `scripts/fixtures/research-openai-models.mjs`, separately from the production profiles. Standard omits reasoning controls for [GPT-4.1](https://developers.openai.com/api/docs/models/gpt-4.1), uses `minimal` for [GPT-5](https://developers.openai.com/api/docs/models/gpt-5), `none` for [Sol](https://developers.openai.com/api/docs/models/gpt-5.6-sol) and [Luna](https://developers.openai.com/api/docs/models/gpt-5.6-luna), and `low` for [Astra](https://developers.openai.com/api/docs/models/gpt-6-astra). Astra's Chat Completions route is valid for this text-only transport; its unsupported sampling/logprob fields are absent, as required by the [migration guide](https://developers.openai.com/api/docs/guides/latest-model?model=gpt-6-astra).

The transport audit passes **206 SDK requests**, in normal and streaming modes, plus two optional-parameter retries. It verifies exact effort, output-token field and sampling controls. All HTTP inference is redirected to a loopback server, and a fetch guard rejects external origins. Another **74 simulated App Server turns** test subscription catalogues, reversed capability order, stale selections, `ultra`, mandatory/optional thinking, and models with missing or empty capabilities. These are synthetic catalogues, not assertions that a subscription currently offers each model or effort. No Codex runtime is launched. Ordinary chat retains its global/API and per-model/Codex reasoning settings.

These checks validate request compatibility against documented contracts without paid inference. They do not test live account availability, quotas, model retirement, or generated-answer quality.
