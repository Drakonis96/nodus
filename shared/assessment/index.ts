/**
 * Assessment engine (teaching vault) — public surface.
 *
 * See ./model.ts for why the numeric grade is a projection rather than the datum,
 * and ./profiles.ts for why every institutional limit is an editable advisory.
 */
export * from './model';
export * from './profiles';
export * from './engine';
